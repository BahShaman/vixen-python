#CalcVixFiles

where is the current directory (CURDIR)

first look at the seq file passed in.
	if its a valid file:
		(meaning its found in the current directory)
		(check if profile in same dir)
		(look up one directory)
	